Here goes all the trained models

For each data set we have 2 directories, one for the bi-directional model
and one for the original one-direction model
At each dierctory you can find a googledrive link that contains the trained model and paramters.